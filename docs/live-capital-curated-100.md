# Capital establishment and curated research: fresh 100-round runbook

## Verified findings

The target remains one fresh100-round game with200 ordered, released seats on
physical display:1/DP-2. Both configured agents use MiniMax-M3. Configuration
`configs/live-hotseat-curated-growth-minimax2-100.yaml` enables effective research
building hints in both existing adaptive-context blocks. A complete configuration
comparison retained in `runs/integration-checks-20260906` allows only the new
match identifier and that opt-in flag to differ from the prior adaptive-growth
profile. Provider identity, seeds, output/request limits, completeness enforcement
and `declare_own_endpath_drift=true` are unchanged.

The prior sourcea941896 passed its1765-test release gate with1optional skip and
full Ruff. Its fresh runminimax100-20260907T024441Z still failed operationally:
P0's T1 adjacent settler move was accepted without position change, a capital
progress review returned only a warrior order, and the later repeated move blocked
continuation. Root stopped it after11 complete rounds/22 released seats, preserving
one matching terminal, completed cleanup and an active P0T12 lease. No watchdog
violation or expired deadline occurred; one informational popup was observed
dismissed.22 movement-allowance audits admitted zero extra rows. The separate
operator-stop record explains the capital failure; the driver summary records
cancellation during recovery. It is neither a100-round pass nor a resumed match.

Authority and preserved evidence:
`/home/alexk/civ-arena-reliability-20260904/runs/minimax100-20260907T024441Z` and
`/home/alexk/civ-arena-reliability-20260904/runs/100-round-live-20260907T024441Z`.
Event SHA256 `c9d996b188d33bad650955efc0e7f7855dfa9b13d7a9d76e67bbc893ee8caf2d`;
unique native save SHA256 `21d67275ea79dbbdb13f145f362e68c683dffaf517f5f7d804a6afed48b11483`.

Research source342a808 had213 affected tests and20 independent probes pass.
The root-executed read-only native qualification at
`/home/alexk/civ-arena-research-briefing-20260907/runs/native-research-qualification-20260907T032544Z`
returned five current research choices exactly matching the legacy read and one
Pottery-to-Granary hint from loaded GameInfo. P0T12 remained unchanged; four reads,
one connection, zero native actions/provider requests, completed cleanup. This
qualifies the current seat's read surface, not all rules, other seats or strategy.

The separately served browser candidate7f3fbe6 passed94 affected tests and14
independent Python probes plus7 offline JavaScript groups. Its physical Chrome
capture on port8792 showed the preserved run, working zoom and the productive
journal, with empty recorded console/page/request-error arrays. Evidence:
`/home/alexk/civ-arena-dashboard-productive-20260907/runs/dashboard-visible-deployment-20260907`.
The map retains dated player observations; district placement/project queue
receipts are historical admission evidence, not proof of completion or current
presence. The active viewer pointer is main runs/sixty-round-development-20260905/
active-dashboard.json. Prepared watchers consume its port8792 URL.

## Follow-up probes

Finish the capital component's focused checks and independent review, then run the
settled integration's full local pytest gate and Ruff once with complete capture.
Keep that reviewed source frozen throughout live execution. Record the final
commit/tree, exact configuration and mod hashes in the unique run's launch record.
The ignored integration report must carry final gate and live outcomes; this
runbook does not claim a future check has passed.

Launch from the final integration worktree after those checks:

```bash
PYTHONPATH=src /home/alexk/documents/civ-arena/.venv/bin/python runs/integration-checks-20260906/launch_fresh_100.py configs/live-hotseat-curated-growth-minimax2-100.yaml 100
PYTHONPATH=src python3 runs/integration-checks-20260906/stage_watchers.py
```

This goes directly to the requested match after normal Architecture-1 startup;
it adds no separate model rehearsal. Startup2700seconds,match7200seconds,
agent600seconds,recovery180seconds/eight sweeps and cleanup20seconds remain.
Native qualification and screenshots are separate from event-log authority.
Observe both first capitals and real productive milestones as well as ordered
closures, requests, UI handoffs and exact admitted movement rows.

Stop-and-preserve at the first unresolved failure. Verify the run-specific
launcherPID, owner, command and cwd. Send one SIGTERM to that launcher only; let
it forward cancellation and wait at least22seconds for driver cleanup. Do not
signal its entire process group, Steam, X or Chrome. Keep all run folders and
create a uniquely named native save after verifying there is no existing tuner
client. Never resume or rename the stopped run into acceptance. Hard kill/storage
failure stays incomplete. Only one tuner client may operate during a match.

## Blocked checks

The final integrated100-round completion is pending. The original two consecutive
fresh30-round acceptance runs remain separately unmet; longer failed prefixes do
not count. Native project admission and construction completion are unproved.

## Evidence gaps

Source/fake checks, provider accounting, physical browser rendering and native
state observations remain distinct. The native research slice does not establish
loaded content identity, complete trait/modifier effects, city conditions or the
best research plan. Declared yields are not projected effective city output.
Productive native readbacks remain outside the mod mutation ledger/digest and
need their separate audit. No calibrated strategic certainty, optimal adjacency,
full-game victory or automatic save recovery is claimed. Publishing is excluded.
